import * as d3 from 'd3';
import { Series } from '../type';
interface UseSeriesProps<D> {
    legends: Array<string>;
    maxValue: number;
    minDisplayPercentage: number;
    labels: Array<string>;
    data: Array<Series<Array<D>>>;
}
declare const useSeries: <D>({ legends, maxValue, minDisplayPercentage, labels, data, }: UseSeriesProps<D>) => d3.Series<{
    label: string;
    data: Record<string, string | number>;
}, string>[];
export default useSeries;
