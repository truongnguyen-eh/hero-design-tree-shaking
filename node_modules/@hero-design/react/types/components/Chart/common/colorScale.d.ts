import * as d3 from 'd3';
import { Theme } from '../../../theme';
import { Palette } from '../../../theme/global/colors';
import { ChartColor } from '../constants';
export declare const getColorFromTheme: (semanticColor: keyof Palette, theme: Theme) => string;
export declare const COLOR_MESSAGE_ERROR = "styleConfig.series should have enough color for all labels";
export declare const useColorScale: (labels: string[], customColors?: Array<{
    label: string;
    color: ChartColor;
}>) => d3.ScaleOrdinal<string, string, never>;
