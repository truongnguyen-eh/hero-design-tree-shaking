import React from 'react';
import { CommonProps } from '../../common';
import { LegendConfig, MatchedSegment, NavigationConfig, Series, TickConfig, XAxisConfig, YAxisConfig, BarConfig, StyleConfig } from '../type';
export declare const ColumnChartDataType: (props: Series<Array<number>>) => Series<number[]>;
declare type DataValue = number | undefined;
export interface ColumnChartProps extends CommonProps {
    /**
     * Data of the chart.
     */
    data: Array<Series<Array<DataValue>>>;
    /**
     * Y axis config.
     */
    yAxisConfig?: Omit<YAxisConfig, 'labels'> & {
        tick?: TickConfig;
    };
    /**
     * X axis config.
     */
    xAxisConfig?: Omit<XAxisConfig, 'maxValue' & 'step'>;
    /**
     * Legend config.
     */
    legendConfig?: LegendConfig;
    /**
     * Bar section config.
     */
    barConfig?: BarConfig<DataValue>;
    /**
     * Navigation button config.
     */
    navigationConfig?: NavigationConfig;
    /**
     * Matched pattern to highlight the bars.
     */
    highlightedItems?: MatchedSegment;
    /**
     * Tooltip renderer.
     */
    tooltipRenderer?: (props: {
        label?: string;
        value?: DataValue;
        legendColor?: string;
        xAxisLabel?: string;
    }) => React.ReactNode;
    /**
     * styleConfig use to custom the style of the chart.
     * * styleConfig must be an object:
     *    * color?: use to custom the legend colors.
     */
    styleConfig?: StyleConfig;
}
declare const ColumnChart: ({ data, xAxisConfig, yAxisConfig, legendConfig, barConfig, "data-test-id": dataTestId, navigationConfig, style, className, id, sx, highlightedItems, tooltipRenderer: _tooltipRenderer, styleConfig, }: ColumnChartProps) => React.JSX.Element;
export default ColumnChart;
