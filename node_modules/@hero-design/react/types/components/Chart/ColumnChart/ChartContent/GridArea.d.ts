import React from 'react';
import { CommonProps } from '../../../common';
interface GridAreaProps extends CommonProps {
    width: number;
    height: number;
    maxValue: number;
    minValue: number;
    highlightMiddleLine?: boolean;
    tickInterval?: number;
}
declare const _default: React.MemoExoticComponent<({ maxValue, minValue, width, height, highlightMiddleLine, tickInterval, "data-test-id": dataTestId, }: GridAreaProps) => React.JSX.Element>;
export default _default;
