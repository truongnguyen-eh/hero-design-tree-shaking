import React from 'react';
import { BarConfig, MatchedSegment, Series, XAxisConfig } from '../../type';
import { CommonProps } from '../../../common';
import { DataValue } from '../../LineChart/types';
declare type BarData = {
    legend: string;
    value: number;
    color: string;
    label: string;
    xAxisLabel: string;
    percent: number;
};
interface BarAreaProps extends CommonProps {
    width: number;
    height: number;
    maxValue: number;
    minValue: number;
    xAxisConfig?: Omit<XAxisConfig, 'maxValue'>;
    highlightedItems?: MatchedSegment;
    barConfig?: BarConfig<DataValue>;
    onMouseOver: (e: PointerEvent, barData: BarData) => void;
    onMouseOut: (e: PointerEvent) => void;
    onMouseMove: (e: PointerEvent) => void;
    selectedLegends: string[];
    data: Array<Series<Array<DataValue>> & {
        color: string;
    }>;
    size?: BarConfig<unknown>['size'];
}
declare const BarArea: ({ xAxisConfig, height, width, maxValue, minValue, "data-test-id": dataTestId, highlightedItems, barConfig, onMouseOver, selectedLegends, data, onMouseOut, onMouseMove, size, }: BarAreaProps) => React.JSX.Element | null;
export default BarArea;
