import React from 'react';
import { BarConfig, XAxisConfig } from '../../type';
interface XAxisProps {
    width: number;
    height: number;
    xAxisConfig?: Omit<XAxisConfig, 'maxValue'>;
    size?: BarConfig<unknown>['size'];
}
declare const _default: React.MemoExoticComponent<({ xAxisConfig, width, height, size }: XAxisProps) => React.JSX.Element>;
export default _default;
