import React from 'react';
import { YAxisConfig } from '../../type';
interface YAxisTitleProps {
    height: number;
    yAxisConfig?: Omit<YAxisConfig, 'maxValue'>;
    setSize: (size: {
        width: number;
        height: number;
    }) => void;
    yAxisWidth: number;
}
declare const _default: React.MemoExoticComponent<({ yAxisConfig, height, setSize, yAxisWidth, }: YAxisTitleProps) => React.JSX.Element>;
export default _default;
