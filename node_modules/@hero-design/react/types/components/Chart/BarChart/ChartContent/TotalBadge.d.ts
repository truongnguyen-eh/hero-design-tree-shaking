import React from 'react';
import { CommonProps } from '../../../common';
interface TotalBadgeProps extends CommonProps {
    total: number;
    totalFormatter?: (totalValue: number) => string;
    x: number;
    y: number;
}
declare const _default: React.MemoExoticComponent<({ total, totalFormatter, className, x, y, "data-test-id": dataTestId, }: TotalBadgeProps) => React.JSX.Element>;
export default _default;
