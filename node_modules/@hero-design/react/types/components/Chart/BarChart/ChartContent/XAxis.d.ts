import React from 'react';
interface XAxisProps {
    width: number;
    height: number;
    maxValue: number;
    minValue: number;
    defaultStep: number;
}
declare const _default: React.MemoExoticComponent<({ maxValue, minValue, width, height, defaultStep, }: XAxisProps) => React.JSX.Element>;
export default _default;
