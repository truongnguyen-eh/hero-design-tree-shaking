import { TickConfig, XAxisConfig, YAxisConfig } from '../type';
export declare type ElementSize = {
    width: number;
    height: number;
};
export declare type LineChartYAxisConfig = Omit<YAxisConfig, 'labels'> & {
    tick?: TickConfig;
    /**
     * Min value of y axis.
     */
    minValue?: number;
};
export declare type DataValue = number | undefined;
export declare type LineChartXAxisConfig = Omit<XAxisConfig, 'maxValue' | 'step'>;
export declare type LineChartVariants = 'outlined' | 'filled';
