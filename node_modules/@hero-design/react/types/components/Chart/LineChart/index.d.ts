import React from 'react';
import { CommonProps } from '../../common';
import { LegendConfig, NavigationConfig, Series, StyleConfig } from '../type';
import { DataValue, LineChartVariants, LineChartXAxisConfig, LineChartYAxisConfig } from './types';
export declare const LineChartDataType: (props: Series<Array<number>>) => Series<number[]>;
export interface LineChartProps extends CommonProps {
    /**
     * Data of the chart.
     */
    data: Array<Series<Array<DataValue>>>;
    /**
     * Y axis config.
     */
    yAxisConfig?: LineChartYAxisConfig;
    /**
     * X axis config.
     */
    xAxisConfig?: LineChartXAxisConfig;
    /**
     * Legend config.
     */
    legendConfig?: LegendConfig;
    /**
     * Navigation button config.
     */
    navigationConfig?: NavigationConfig;
    /**
     * Chart variants.
     */
    variant?: LineChartVariants;
    /**
     * * styleConfig use to custom the style of the chart.
     * * styleConfig must be an object:
     *    * color?: use to custom the legend colors.
     */
    styleConfig?: StyleConfig;
}
declare const LineChart: ({ data, xAxisConfig, yAxisConfig, legendConfig, "data-test-id": dataTestId, navigationConfig, style, className, id, sx, variant, styleConfig, }: LineChartProps) => React.JSX.Element;
export default LineChart;
