import React from 'react';
import { TickConfig, XAxisConfig } from '../../type';
interface XAxisProps {
    width: number;
    height: number;
    maxValue: number;
    minValue: number;
    xAxisConfig?: Omit<XAxisConfig, 'labels'> & {
        tick?: TickConfig;
    };
}
declare const _default: React.MemoExoticComponent<({ maxValue, minValue, width, height, xAxisConfig, }: XAxisProps) => React.JSX.Element>;
export default _default;
