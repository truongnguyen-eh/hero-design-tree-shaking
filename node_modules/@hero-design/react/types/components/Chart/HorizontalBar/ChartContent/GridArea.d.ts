import React from 'react';
import { TickConfig, XAxisConfig } from '../../type';
interface GridAreaProps {
    width: number;
    height: number;
    maxValue: number;
    minValue: number;
    xAxisConfig: Omit<XAxisConfig, 'labels'> & {
        tick?: TickConfig;
    };
    highlightMiddleLine?: boolean;
}
declare const _default: React.MemoExoticComponent<({ xAxisConfig, maxValue, minValue, width, height, highlightMiddleLine, }: GridAreaProps) => React.JSX.Element>;
export default _default;
