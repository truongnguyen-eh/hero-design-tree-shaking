import React from 'react';
import * as d3 from 'd3';
import { LegendConfig } from '../../type';
import { CommonProps } from '../../../common';
interface LegendGroupProps extends CommonProps {
    legendConfig?: LegendConfig;
    selectedLegends: string[];
    onChange: (selectedLegends: string[]) => void;
    legends: string[];
    colorScale: d3.ScaleOrdinal<string, string, never>;
}
declare const LegendGroup: ({ legendConfig, "data-test-id": dataTestId, selectedLegends, onChange, legends, colorScale, }: LegendGroupProps) => React.JSX.Element | null;
export default LegendGroup;
