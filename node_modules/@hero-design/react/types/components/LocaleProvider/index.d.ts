import React from 'react';
import { LocaleValues } from '../../locales/types';
interface LocaleProviderProps {
    locale: LocaleValues;
}
declare const LocaleProvider: (props: React.PropsWithChildren<LocaleProviderProps>) => React.JSX.Element | null;
export default LocaleProvider;
