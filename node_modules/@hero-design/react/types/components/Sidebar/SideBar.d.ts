import type { ReactElement } from 'react';
import type { SideBarItem } from './types';
import type { CommonProps } from '../common';
export interface SideBarProps extends CommonProps {
    /**
     * List of side bar items.
     */
    items: {
        header: SideBarItem[];
        body: SideBarItem[];
        footer: SideBarItem[];
    };
    /**
     * Selected menu item ID.
     */
    selectedItemId?: string;
    /**
     * Event handler for clicking on side bar items.
     */
    onClickItem: (item: SideBarItem) => void;
}
declare const SideBar: ({ items, selectedItemId, onClickItem, id, className, style, sx, "data-test-id": dataTestId, }: SideBarProps) => ReactElement;
export default SideBar;
