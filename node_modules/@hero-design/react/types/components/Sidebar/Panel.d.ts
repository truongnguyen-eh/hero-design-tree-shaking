import React from 'react';
import type { ReactElement } from 'react';
import type { CommonProps } from '../common';
import type { PanelItem } from './types';
export interface PanelProps extends CommonProps {
    /**
     * Panel heading.
     */
    heading: string;
    /**
     * Selected menu item ID.
     */
    selectedItemId?: string;
    /**
     * Event handler for clicking on panel items.
     */
    onClickItem: (item?: PanelItem) => void;
    /**
     * List of panel items.
     */
    items: {
        [key: string]: PanelItem[];
    };
    variant?: 'blank' | 'menu';
    children?: React.ReactNode;
}
declare const Panel: ({ heading, items, selectedItemId, onClickItem, variant, children, id, className, style, sx, "data-test-id": dataTestId, }: PanelProps) => ReactElement;
export default Panel;
