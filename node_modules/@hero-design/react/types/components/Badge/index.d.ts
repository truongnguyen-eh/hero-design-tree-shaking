import React from 'react';
import type { ReactElement } from 'react';
import type { CommonProps } from '../common';
export interface StandaloneBadgeProps extends CommonProps {
    /**
     * Content to be displayed, can be number or string.
     */
    content: number | string;
    /**
     * Visual intent color to apply to badge.
     */
    intent?: 'basic' | 'success' | 'primary' | 'warning' | 'danger' | 'error' | 'archived';
    /**
     * Badge variant.
     */
    variant?: 'filled' | 'outlined';
}
declare const Badge: {
    ({ content, intent, variant, id, className, style, sx, "data-test-id": dataTestId, }: StandaloneBadgeProps): ReactElement;
    Status: ({ children, intent, }: import("./Status").StatusBadgeProps) => React.ReactElement<any, string | React.JSXElementConstructor<any>>;
    Count: ({ children, content, intent, }: import("./Count").CountBadgeProps) => React.ReactElement<any, string | React.JSXElementConstructor<any>>;
};
export default Badge;
