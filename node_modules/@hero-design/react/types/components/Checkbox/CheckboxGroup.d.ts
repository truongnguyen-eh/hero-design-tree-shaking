import type { ReactElement } from 'react';
import type { CommonProps } from '../common';
export interface Option<T extends string | number> {
    disabled?: boolean;
    text: string | ReactElement;
    value: T;
}
export interface CheckboxGroupProps<T extends string | number> extends CommonProps {
    /**
     * Layout to render checkboxes.
     */
    layout?: 'vertical' | 'horizontal';
    /**
     * Checkbox group name, used for form submission.
     */
    name?: string;
    /**
     * Change event handler.
     */
    onChange: (value: T[]) => void;
    /**
     * An array of checkbox options to be selected. Shape of an option:
     * {
     *   disabled?: boolean;
     *   text: string | ReactElement;
     *   value: T;
     * }
     */
    options: Option<T>[];
    /**
     * Selected value. T is a generic type.
     */
    value: T[];
}
declare const CheckboxGroup: <T extends string | number>({ name, value, options, onChange, layout, id, className, style, sx, "data-test-id": dataTestId, }: CheckboxGroupProps<T>) => ReactElement;
export default CheckboxGroup;
