import type { ReactElement, MouseEvent } from 'react';
declare const StepIcon: ({ clickable, status, active, onClick, }: {
    active: boolean;
    clickable: boolean;
    onClick?: ((e: MouseEvent) => void) | undefined;
    status: 'complete' | 'incomplete';
}) => ReactElement;
export default StepIcon;
