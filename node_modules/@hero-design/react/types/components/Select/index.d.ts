import type { ReactElement } from 'react';
import MultiSelect from './MultiSelect';
import type { SelectProps } from './SingleSelect';
import type { BaseOption } from './types';
declare type CompoundSelectComponent = (<V extends string | number, T extends BaseOption<V>>(props: SelectProps<V, T>) => ReactElement) & {
    Multi: typeof MultiSelect;
};
declare const CompoundSelect: CompoundSelectComponent;
export default CompoundSelect;
