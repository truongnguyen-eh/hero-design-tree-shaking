import React from 'react';
import type { UseComboboxReturnValue } from 'downshift';
import type { CSSProperties } from 'react';
import type { GroupedOption, BaseOption } from '../types';
import type { SelectProps } from '.';
declare type OptionListComponentProps<V extends string | number, T extends BaseOption<V>> = {
    categories: GroupedOption<V, T>[];
    hasResults: boolean;
    newOption: BaseOption<V>;
    selectedItem: T | undefined;
    style: CSSProperties;
} & Pick<UseComboboxReturnValue<BaseOption<V>>, 'getMenuProps' | 'getItemProps' | 'highlightedIndex'> & Pick<SelectProps<V, T>, 'optionRenderer' | 'loading' | 'onScrollListToBottom' | 'onCreateNewOption' | 'noResults'>;
declare const OptionList: <V extends string | number, T extends BaseOption<V>>({ categories, hasResults, newOption, selectedItem, getItemProps, getMenuProps, highlightedIndex, onScrollListToBottom, onCreateNewOption, loading, noResults, optionRenderer, style, }: OptionListComponentProps<V, T>) => React.JSX.Element;
export default OptionList;
