import type { ReactElement } from 'react';
interface SuffixIconProps<V extends string | number> {
    loading?: boolean;
    onChange?: (value?: V) => void;
    open: boolean;
    removeShown?: boolean;
}
declare const SuffixIcon: <V extends string | number>({ loading, onChange, open, removeShown, }: SuffixIconProps<V>) => ReactElement;
export default SuffixIcon;
