import { ReactNode } from 'react';
import type { ReactElement } from 'react';
import type { CommonProps } from '../common';
export interface CardContentProps extends CommonProps {
    /**
     * Custom content.
     */
    children?: ReactNode;
    /**
     * Short hand for Card's content.
     */
    content?: ReactNode;
    /**
     * Format as extra content.
     */
    extra?: boolean;
}
declare const CardContent: ({ content, extra, children, id, className, style, sx, "data-test-id": dataTestId, }: CardContentProps) => ReactElement;
export default CardContent;
