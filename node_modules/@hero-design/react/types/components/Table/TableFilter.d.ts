import type { ReactNode, ReactElement } from 'react';
interface TableFilterProps {
    'data-test-id'?: string;
    hasFilterInput: boolean;
    renderFilter: (props: {
        onClose: () => void;
    }) => ReactNode;
}
declare const TableFilter: ({ renderFilter, hasFilterInput, "data-test-id": dataTestId, }: TableFilterProps) => ReactElement;
export default TableFilter;
