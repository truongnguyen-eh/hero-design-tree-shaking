import React from 'react';
import type { FilterValues } from './types';
import type { FilterProps } from './Filter';
import type { FiltersProps } from './Filters';
declare const createFilters: <T extends FilterValues>() => {
    Filters: (props: FiltersProps<T>) => React.JSX.Element;
    Filter: <K extends keyof T>(props: FilterProps<T, K>) => React.JSX.Element;
};
export default createFilters;
