import React from 'react';
import type { ReactElement } from 'react';
import type { FilterValues } from './types';
import type { FilterProps } from './Filter';
declare const GroupedFilter: <T extends FilterValues, K extends keyof T>({ label, filters, }: {
    filters: React.ReactElement<FilterProps<T, K>, string | React.JSXElementConstructor<any>>[];
    label: string;
}) => React.JSX.Element;
export default GroupedFilter;
