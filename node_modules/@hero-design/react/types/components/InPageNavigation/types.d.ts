/// <reference types="react" />
import { IconButtonProps } from '../Button/IconButton';
import type { IconName, IconIntent } from '../Icon';
declare type IndicatorIcon = {
    icon: IconName;
    intent?: IconIntent;
};
declare type ActionIcon = {
    onClick: (e: React.MouseEvent<Element, MouseEvent>) => void;
    icon: IconName;
    intent?: IconButtonProps['intent'];
};
declare type InPageNavItem = {
    id: string;
    text: string;
    url?: string;
    disabled?: boolean;
    disabledReason?: string;
    prefix?: IndicatorIcon;
    suffix?: IndicatorIcon;
    actions?: Array<ActionIcon>;
};
export type { InPageNavItem };
