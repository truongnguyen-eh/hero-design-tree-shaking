import type { ReactElement } from 'react';
export interface DoubleCalendarProps {
    dateClickCount: number;
    endDate?: Date;
    isSettingEndDate: boolean;
    isSettingStartDate: boolean;
    maxDate?: Date;
    minDate?: Date;
    onSelectDate?: (date: Date) => void;
    startDate?: Date;
}
declare const DoubleCalendar: ({ startDate, endDate, minDate, maxDate, onSelectDate, isSettingStartDate, isSettingEndDate, dateClickCount, }: DoubleCalendarProps) => ReactElement;
export default DoubleCalendar;
