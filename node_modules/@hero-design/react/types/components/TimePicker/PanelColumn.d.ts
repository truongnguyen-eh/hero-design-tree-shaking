import React from 'react';
import type { ReactElement } from 'react';
export declare type Unit = {
    label: React.ReactText;
    value: number;
};
export declare type PanelColumnProps = {
    onSelect?: (value: number) => void;
    units?: Unit[];
    value?: number;
};
declare const PanelColumn: (props: PanelColumnProps) => ReactElement;
export default PanelColumn;
