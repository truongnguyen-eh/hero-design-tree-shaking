import { ReactElement, ReactNode } from 'react';
import { IconName } from '../../Icon';
export declare type SubTabItem = {
    disabled?: boolean;
    icon?: IconName | ReactElement;
    id: string | number;
    panel: ReactNode;
    title: ReactNode;
};
export declare type SelectedSubTabId = string | number;
