import React, { ReactNode } from 'react';
import { SelectedSubTabId, SubTabItem } from './types';
declare const OverflowTabList: ({ tabs, selectedTabId, onChange, id, focusTabIndex, onChangeFocusTabIndex, viewMoreText: _viewMoreText, tabWidths, size, }: {
    tabs: SubTabItem[];
    selectedTabId: string | number;
    onChange: (id: SelectedSubTabId) => void;
    id: SelectedSubTabId;
    focusTabIndex: number;
    onChangeFocusTabIndex: (tabIndex: number) => void;
    viewMoreText?: ReactNode;
    tabWidths: number[];
    size?: "small" | "medium" | undefined;
}) => React.JSX.Element;
export default OverflowTabList;
