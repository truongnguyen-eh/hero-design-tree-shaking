import React from 'react';
import type { ReactElement, ChangeEvent } from 'react';
import type { CommonProps } from '../common';
export interface RateIconProps<T extends string | number> extends Omit<CommonProps, 'id'> {
    active: boolean;
    checked: boolean;
    disabled: boolean;
    name?: string;
    onChange: (e: ChangeEvent<HTMLInputElement>) => void;
    setHoverValue: React.Dispatch<React.SetStateAction<T | null>>;
    size: 'medium' | 'large';
    value: T;
}
declare const RateIcon: <T extends string | number>({ name, value, active, disabled, checked, onChange, setHoverValue, size, }: RateIconProps<T>) => ReactElement;
export default RateIcon;
