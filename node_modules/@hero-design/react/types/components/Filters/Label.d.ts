import React, { ReactElement } from 'react';
import { IconName } from '../Icon';
export interface FilterLabelProps {
    icon?: IconName | ReactElement;
    text: string;
}
declare const Label: {
    ({ icon, text }: FilterLabelProps): React.JSX.Element;
    displayName: string;
};
export default Label;
