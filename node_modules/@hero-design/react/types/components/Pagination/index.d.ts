import type { ReactElement } from 'react';
import type { CommonProps } from '../common';
export interface PaginationProps extends CommonProps {
    /**
     * Current selected page. This is 1-based indexing.
     */
    current: number;
    /**
     * Change event handler receiving new selected page.
     */
    onChange: (selectedPage: number) => void;
    /**
     * Total number of pages.
     */
    total: number;
    /**
     * Type of pagination.
     */
    variant?: 'basic' | 'compact';
}
declare const Pagination: ({ current, total, onChange, variant, id, className, style, sx, "data-test-id": dataTestId, }: PaginationProps) => ReactElement;
export default Pagination;
