import React from 'react';
import type { AriaAttributes, ReactElement, ReactNode } from 'react';
import type { CommonProps, SystemProperties } from '../common';
export interface BoxProps extends SystemProperties, CommonProps, AriaAttributes {
    /**
     * Box's children node.
     */
    children?: ReactNode;
    /**
     * Set the handler to handle `click` event.
     */
    onClick?: React.MouseEventHandler<HTMLElement>;
    /**
     * You can use the tagName prop to change the element render, just like styled-components.
     */
    tagName?: keyof JSX.IntrinsicElements;
}
declare const Box: ({ bgColor, borderColor, borderRadius, borderStyle, borderWidth, boxShadow, children, color, fontSize, fontWeight, lineHeight, m, mb, ml, mr, mt, p, pb, pl, pr, pt, sx, className, id, style, "data-test-id": dataTestId, onClick, tagName, ...ariaProps }: BoxProps) => ReactElement;
export default Box;
