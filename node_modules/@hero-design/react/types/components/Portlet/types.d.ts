import { IconButtonProps } from '../Button/IconButton';
import { MenuItemProps } from '../Menu/MenuItem';
export declare type PortletActionMenu = {
    menus: {
        divider?: 'top' | 'bottom';
        value: string;
        item: MenuItemProps;
    }[];
};
export declare type PorletAction = IconButtonProps & Partial<PortletActionMenu>;
