import type { ReactElement } from 'react';
import type { CommonProps } from '../../common';
export interface SingleSliderProps extends CommonProps {
    /**
     * Whether the slider's tooltip is always shown.
     */
    alwaysShowTooltip?: boolean;
    /**
     * Custom text for tooltip. If defined, it will replace the auto generated tooltip.
     */
    customTooltipText?: string;
    /**
     * Whether the slider is disabled.
     */
    disabled?: boolean;
    /**
     * Slider max value.
     */
    max?: number;
    /**
     * Slider min value.
     */
    min?: number;
    /**
     * onChange event handler.
     */
    onChange?: (value: number) => void;
    /**
     * Slider step.
     */
    step?: number;
    /**
     * Current slider's value.
     */
    value: number;
}
declare const SingleSlider: ({ min, max, step, value, disabled, alwaysShowTooltip, customTooltipText, id, className, style, sx, "data-test-id": dataTestId, onChange, }: SingleSliderProps) => ReactElement;
export default SingleSlider;
