import type { ReactElement } from 'react';
import type { CommonProps } from '../common';
export interface EmptyProps extends CommonProps {
    /**
     * Extra action buttons.
     */
    extra?: ReactElement;
    /**
     *  Empty image source URL.
     */
    imageSrc?: string;
    /**
     * Size of the Empty.
     */
    size?: 'small' | 'medium';
    /**
     *  Text under image.
     */
    text: string | ReactElement;
    /**
     *  Optional heading text under image.
     */
    title?: string | ReactElement;
}
declare const Empty: ({ imageSrc, text, title, extra, size, id, className, style, sx, "data-test-id": dataTestId, }: EmptyProps) => ReactElement;
export default Empty;
