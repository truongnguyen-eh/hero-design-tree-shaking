import type { GlobalTheme } from '../global';
declare const getLegendTheme: (theme: GlobalTheme) => {
    radii: {
        legendCircle: string;
    };
    space: {
        legendCircleMarginRight: string;
    };
    sizes: {
        legendWidth: string;
        legendHeight: string;
    };
};
declare type LegendTheme = ReturnType<typeof getLegendTheme>;
export { getLegendTheme };
export type { LegendTheme };
