import { heroSystemPalette, omopSystemPalette, eBensSystemPalette, jobsSystemPalette } from './colors';
import { scale as defaultScale } from './scales';
import type { Sizes } from './sizes';
import type { Space } from './space';
import type { FontSizes, FontWeights, LineHeights } from './typography';
import type { BorderWidths, Radii } from './borders';
import type { Shadows } from './shadows';
import type { SystemPalette, Palette } from './colors';
import type { Scale } from './scales';
import { Breakpoints } from './breakpoints';
declare type GlobalTheme = {
    borderWidths: BorderWidths;
    breakpoints: Breakpoints;
    colors: SystemPalette & {
        palette: Palette;
    };
    fontSizes: FontSizes;
    fontWeights: FontWeights;
    lineHeights: LineHeights;
    radii: Radii;
    shadows: Shadows;
    sizes: Sizes;
    space: Space;
};
declare const getGlobalTheme: (scale: Scale, systemPalette: SystemPalette) => GlobalTheme;
export { defaultScale, heroSystemPalette, omopSystemPalette, eBensSystemPalette, jobsSystemPalette, getGlobalTheme, };
export type { GlobalTheme };
