import type { SystemPalette } from './colors';
declare const getShadows: (systemPalette: SystemPalette) => {
    default: string;
    primary: string;
    danger: string;
    warning: string;
    divider: string;
};
declare type Shadows = ReturnType<typeof getShadows>;
export { getShadows };
export type { Shadows };
