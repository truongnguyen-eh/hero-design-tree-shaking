export interface NonEmptyArray<A> extends Array<A> {
  0: A;
}

export const create = <A>(a: A, ...othersA: Array<A>) =>
  [a, ...othersA] as NonEmptyArray<A>;

export const isNonEmptyArray = <A>(arr: Array<A>): arr is NonEmptyArray<A> =>
  arr.length > 0;
