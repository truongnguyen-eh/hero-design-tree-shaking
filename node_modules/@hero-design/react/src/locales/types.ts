const LOCALES = ['en-AU', 'vi-VN'] as const;

// eslint-disable-next-line prettier/prettier
export type LocaleCode = (typeof LOCALES)[number];

export type LocaleValues = {
  lang: LocaleCode;
  translations: { [key: string]: string };
};
