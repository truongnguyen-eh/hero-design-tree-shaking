export type PossibleFilterValue =
  | undefined
  | string
  | number
  | (string | number)[]
  | { endDate?: string; startDate?: string };

export type FilterValues = Record<string, PossibleFilterValue>;
