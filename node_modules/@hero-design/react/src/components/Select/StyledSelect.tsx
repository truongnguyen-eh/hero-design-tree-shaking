import styled from 'styled-components';

import { DropdownWrapper } from '../Dropdown/StyledDropdown';

const SelectWrapper = styled.div`
  position: relative;
  margin: 0;
  padding: 0;

  &:focus {
    outline: none;
  }

  & > ${DropdownWrapper} {
    display: block;
  }
`;

const MenuWrapper = styled.div`
  overflow-y: auto;
  max-height: ${({ theme }) => theme.__hd__.select.sizes.menuMaxHeight};
  margin: 0;
  padding: 0;
`;

const CategoryWrapper = styled.div`
  margin: 0;
  padding: ${({ theme }) => theme.__hd__.select.space.menuCategoryPadding};
  font-size: ${({ theme }) => theme.__hd__.select.fontSizes.menuCategory};
  line-height: ${({ theme }) => theme.__hd__.select.lineHeights.menuCategory};
`;

const HelpTextWrapper = styled.div`
  display: flex;
  margin: 0;
  padding: 0;
  align-items: center;
`;

const CheckmarkWrapper = styled.div<{ shown: boolean }>`
  display: inline-flex;
  margin: 0;
  padding: 0;
  margin-left: ${({ theme }) =>
    theme.__hd__.select.space.menuCheckmarkMarginLeft};
  opacity: ${({ shown }): number => (shown ? 1 : 0)};
`;

const StyledSuffixIconWrapper = styled.div`
  display: flex;
  align-items: center;
  margin: 0;
  padding: 0;
`;

const StyledLoadingIconWrapper = styled.div`
  display: flex;
  margin: 0;
  padding: 0;
  padding-right: ${({ theme }) =>
    theme.__hd__.select.space.loadingIconPaddingRight};
`;

export {
  SelectWrapper,
  MenuWrapper,
  CategoryWrapper,
  HelpTextWrapper,
  CheckmarkWrapper,
  StyledSuffixIconWrapper,
  StyledLoadingIconWrapper,
};
