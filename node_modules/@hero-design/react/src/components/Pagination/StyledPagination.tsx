import styled, { css } from 'styled-components';

const PaginationWrapper = styled.div`
  display: flex;
  margin: 0;
  padding: 0;
  align-items: center;
`;

const PageNumber = styled.button<{
  themeDisplayAs:
    | 'pageNumber'
    | 'activePage'
    | 'spreading'
    | 'disabled'
    | 'navigator';
}>`
  display: flex;
  align-items: center;
  justify-content: center;
  height: ${({ theme }) => theme.__hd__.pagination.sizes.height};
  min-width: ${({ theme }) => theme.__hd__.pagination.sizes.minWidth};
  margin: 0;
  padding: 0 ${({ theme }) => theme.__hd__.pagination.space.leftRightPadding};
  font-size: ${({ theme }) => theme.__hd__.pagination.fontSizes.default};
  font-weight: ${({ theme }) => theme.__hd__.pagination.fontWeights.default};
  border-radius: ${({ theme }) => theme.__hd__.pagination.radii.default};
  background: ${({ theme }) => theme.__hd__.pagination.colors.background};
  color: ${({ theme }) => theme.__hd__.pagination.colors.number};
  cursor: pointer;

  &:focus {
    outline: none;
  }

  ${({ themeDisplayAs, theme }) => {
    switch (themeDisplayAs) {
      case 'pageNumber':
        return css`
          border: none;
          &:hover {
            color: ${theme.__hd__.pagination.colors.numberActive};
          }
        `;
      case 'activePage':
        return css`
          color: ${theme.__hd__.pagination.colors.numberActive};
          border-width: ${theme.__hd__.pagination.borderWidths.default};
          border-style: solid;
          border-color: ${theme.__hd__.pagination.colors.numberActive};
        `;
      case 'spreading':
        return css`
          border: none;
          pointer-events: none;
        `;
      case 'disabled':
        return css`
          border: none;
          color: ${theme.__hd__.pagination.colors.numberDisabled};
          pointer-events: none;
        `;
      case 'navigator':
        return css`
          border: none;
        `;
    }
  }};
`;

export { PaginationWrapper, PageNumber };
