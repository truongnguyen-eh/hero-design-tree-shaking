import React from 'react';
import styled, { css } from 'styled-components';
import type { ReactElement } from 'react';
import type { CommonProps } from '../../common';

export const HEADINGS = {
  1: 'h1',
  2: 'h2',
  3: 'h3',
  4: 'h4',
  5: 'h5',
};

const Heading = ({
  themeLevel,
  children,
  id,
  className,
  style,
  'data-test-id': dataTestId,
}: {
  children: string | ReactElement;
  className?: string;
  themeLevel: 1 | 2 | 3 | 4 | 5;
} & CommonProps): ReactElement => {
  return React.createElement(
    HEADINGS[themeLevel],
    { className, style, id, 'data-test-id': dataTestId },
    children
  );
};

const StyledTitle = styled(Heading)<{
  themeFontWeight: 'light' | 'regular' | 'semi-bold' | 'bold';
  themeIntent: 'main' | 'subdued';
  themeLevel: 1 | 2 | 3 | 4 | 5;
}>`
  margin: 0;
  padding: 0;

  ${({ themeIntent, theme }) => {
    switch (themeIntent) {
      case 'main':
        return css`
          color: ${theme.__hd__.typography.colors.main};
        `;
      case 'subdued':
        return css`
          color: ${theme.__hd__.typography.colors.subdued};
        `;
    }
  }};

  ${({ themeFontWeight, theme }) => {
    switch (themeFontWeight) {
      case 'light':
        return css`
          font-weight: ${theme.__hd__.typography.fontWeights.light};
        `;
      case 'regular':
        return css`
          font-weight: ${theme.__hd__.typography.fontWeights.regular};
        `;
      case 'semi-bold':
        return css`
          font-weight: ${theme.__hd__.typography.fontWeights.semiBold};
        `;
      case 'bold':
        return css`
          font-weight: ${theme.__hd__.typography.fontWeights.bold};
        `;
    }
  }};

  ${({ themeLevel, theme }) => {
    switch (themeLevel) {
      case 1:
        return css`
          font-size: ${theme.__hd__.typography.fontSizes.xxxxxlarge};
          line-height: ${theme.__hd__.typography.lineHeights.xxxxxlarge};
        `;
      case 2:
        return css`
          font-size: ${theme.__hd__.typography.fontSizes.xxxxlarge};
          line-height: ${theme.__hd__.typography.lineHeights.xxxxlarge};
        `;
      case 3:
        return css`
          font-size: ${theme.__hd__.typography.fontSizes.xxxlarge};
          line-height: ${theme.__hd__.typography.lineHeights.xxxlarge};
        `;
      case 4:
        return css`
          font-size: ${theme.__hd__.typography.fontSizes.xxlarge};
          line-height: ${theme.__hd__.typography.lineHeights.xxlarge};
        `;
      case 5:
        return css`
          font-size: ${theme.__hd__.typography.fontSizes.xlarge};
          line-height: ${theme.__hd__.typography.lineHeights.xlarge};
        `;
    }
  }};
`;

export default StyledTitle;
