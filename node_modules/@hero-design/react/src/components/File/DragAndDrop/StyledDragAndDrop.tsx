import styled, { css } from 'styled-components';

export const HiddenInput = styled.input`
  display: none;
`;

export const DropzoneWrapper = styled.div<{ themeActive: boolean }>`
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  cursor: not-allowed;
  min-height: ${({ theme }) => theme.__hd__.file.sizes.dragAndDropMinHeight};
  margin: 0;
  padding: ${({ theme }) => theme.__hd__.file.space.dragAndDropPadding};
  border-radius: ${({ theme }) => theme.__hd__.file.radii.dragAndDrop};
  border: dotted 1px
    ${({ theme }) => theme.__hd__.file.colors.dragAndDropBorder};
  background-color: ${({ theme }) =>
    theme.__hd__.file.colors.dragAndDropBackground};

  ${({ theme, themeActive }) => {
    if (themeActive) {
      return css`
        cursor: pointer;

        &:hover {
          border-color: ${theme.__hd__.file.colors.dragAndDropBorderHover};
        }
      `;
    }
  }};

  div {
    margin: 0;
    padding: 0;
  }
`;
