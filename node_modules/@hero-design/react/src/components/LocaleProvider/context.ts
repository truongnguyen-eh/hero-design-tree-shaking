import { createContext } from 'react';
import enAUTranslations from '../../locales/en_AU';
import { LocaleValues } from '../../locales/types';

const LocaleContext = createContext<LocaleValues>(enAUTranslations);

export { LocaleContext };
