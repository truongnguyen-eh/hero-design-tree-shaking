import styled from 'styled-components';

const StyledChartContainer = styled.div`
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  width: fit-content;
  background-color: ${({ theme }) => theme.__hd__.chart.colors.pie.background};
`;

const StyledMultiplePieChartContainer = styled.div<{
  themeSize: 'small' | 'medium' | 'large';
}>`
  position: relative;
  width: ${({ theme, themeSize }) => theme.__hd__.chart.sizes.pie[themeSize]}px;
  height: ${({ theme, themeSize }) =>
    theme.__hd__.chart.sizes.pie[themeSize]}px;
`;

export { StyledMultiplePieChartContainer, StyledChartContainer };
