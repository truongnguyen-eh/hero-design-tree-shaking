import { TickConfig, XAxisConfig, YAxisConfig } from '../type';

export type ElementSize = {
  width: number;
  height: number;
};

export type LineChartYAxisConfig = Omit<YAxisConfig, 'labels'> & {
  tick?: TickConfig;
  /**
   * Min value of y axis.
   */
  minValue?: number;
};

export type DataValue = number | undefined;
export type LineChartXAxisConfig = Omit<XAxisConfig, 'maxValue' | 'step'>;

export type LineChartVariants = 'outlined' | 'filled';
