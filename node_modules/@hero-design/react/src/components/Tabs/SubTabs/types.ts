import { ReactElement, ReactNode } from 'react';
import { IconName } from '../../Icon';

export type SubTabItem = {
  disabled?: boolean;
  icon?: IconName | ReactElement;
  id: string | number;
  panel: ReactNode;
  title: ReactNode;
};

export type SelectedSubTabId = string | number;
