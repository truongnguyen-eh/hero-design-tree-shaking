import styled from 'styled-components';

const Wrapper = styled.div<{
  alreadyOpen: boolean;
  contentHeight: number;
  open: boolean;
}>`
  height: ${(props): number => (props.open ? props.contentHeight : 0)}px;
  transition: height 0.3s ease;
  overflow: ${({ alreadyOpen }): string =>
    alreadyOpen === true ? 'unset' : 'hidden'};
  margin: 0;
  padding: 0;
`;

const Container = styled.div`
  margin: 0;
  padding: 0;
`;

export { Wrapper, Container };
