import styled from 'styled-components';

export const PageHeaderWrapper = styled.div`
  margin: 0;
  padding: 0;
`;

export const TitleWrapper = styled.div`
  margin: ${({ theme }) => theme.__hd__.pageHeader.space.titleMargin};
  padding: 0;
  font-size: ${({ theme }) => theme.__hd__.pageHeader.fontSizes.title};
  font-weight: ${({ theme }) => theme.__hd__.pageHeader.fontWeights.title};
  color: ${({ theme }) => theme.__hd__.pageHeader.colors.title};
`;
