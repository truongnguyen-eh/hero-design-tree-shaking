import React from 'react';
import { IconButtonProps } from '../Button/IconButton';
import { StyledContextPanelCloseIcon } from './StyledContextPanel';

export type ContextPanelCloseButtonProps = Omit<
  IconButtonProps,
  'icon' | 'size'
>;

const ContextPanelCloseButton = (props: ContextPanelCloseButtonProps) => {
  return <StyledContextPanelCloseIcon icon="cancel" size="small" {...props} />;
};

export default ContextPanelCloseButton;
