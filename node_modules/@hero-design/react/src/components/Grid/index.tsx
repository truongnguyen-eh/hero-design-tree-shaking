import type { FunctionComponent } from 'react';
import Col from './Col';
import Grid from './Grid';
import Row from './Row';
import type { GridProps } from './Grid';

interface CompoundGridProps extends FunctionComponent<GridProps> {
  Col: typeof Col;
  Row: typeof Row;
}

const CompoundGrid = Grid as CompoundGridProps;

CompoundGrid.Col = Col;
CompoundGrid.Row = Row;

export default CompoundGrid;
