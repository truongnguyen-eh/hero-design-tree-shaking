import type { FunctionComponent } from 'react';
import SideBar from './SideBar';
import Panel from './Panel';
import type { SideBarProps } from './SideBar';

interface CompoundSidebarProps extends FunctionComponent<SideBarProps> {
  Panel: typeof Panel;
}

const CompoundSideBar = SideBar as CompoundSidebarProps;

CompoundSideBar.Panel = Panel;

export default CompoundSideBar;
