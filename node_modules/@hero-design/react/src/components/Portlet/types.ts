import { IconButtonProps } from '../Button/IconButton';
import { MenuItemProps } from '../Menu/MenuItem';

export type PortletActionMenu = {
  menus: {
    divider?: 'top' | 'bottom';
    value: string;
    item: MenuItemProps;
  }[];
};

export type PorletAction = IconButtonProps & Partial<PortletActionMenu>;
