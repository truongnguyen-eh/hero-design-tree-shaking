import { FunctionComponent } from 'react';
import ProgressCircle from './ProgressCircle';
import ProgressBar, { ProgressBarProps } from './ProgressBar';

interface CompoundProgressProps extends FunctionComponent<ProgressBarProps> {
  Circle: typeof ProgressCircle;
}

const CompoundProgress = ProgressBar as CompoundProgressProps;
CompoundProgress.Circle = ProgressCircle;

export default CompoundProgress;
