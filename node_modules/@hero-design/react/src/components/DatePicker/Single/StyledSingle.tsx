import styled from 'styled-components';
import Select from '../../Select/SingleSelect';

const StyledSingleSelect = styled(Select)`
  width: ${({ theme }) => theme.__hd__.datePicker.sizes.singleSelectWidth};
`;

export { StyledSingleSelect };
