import Group from './Group';
import Filter from './Filter';

type GroupType = typeof Group;

interface CompoundGroupType extends GroupType {
  Filter: typeof Filter;
}

const CompoundGroup = Group as CompoundGroupType;
CompoundGroup.Filter = Filter;

export default CompoundGroup;
