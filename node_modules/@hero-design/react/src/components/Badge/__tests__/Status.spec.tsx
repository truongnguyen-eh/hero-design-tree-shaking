import React from 'react';
import renderWithTheme from '../../../testUtils/renderWithTheme';

import Status from '../Status';

describe('rendering', () => {
  it('renders status content', () => {
    const { queryByText } = renderWithTheme(<Status>Status content</Status>);

    expect(queryByText('Status content')).toBeInTheDocument();
  });
});
