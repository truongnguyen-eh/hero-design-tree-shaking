export const isNumber = (content: number | string): content is number =>
  Number.isInteger(content);

export const getPaddingState = (
  content: number | string
): 'narrowContent' | 'wideContent' =>
  isNumber(content) && content < 10 ? 'narrowContent' : 'wideContent';

export const MAX_NUMBER = 99;
