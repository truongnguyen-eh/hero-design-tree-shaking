import { defaultScale, eBensSystemPalette } from './global';

import getTheme from './getTheme';

import type { Theme } from './types';

const getEBensTheme = (
  scale = defaultScale,
  systemPalette = eBensSystemPalette
): Theme => getTheme(scale, systemPalette);

export default getEBensTheme;
