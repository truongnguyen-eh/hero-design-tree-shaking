import { jobsWebPalette as palette } from '@hero-design/colors';
import heroSystemPalette from './hero';
import type { SystemPalette } from './types';

const jobsSystemPalette: SystemPalette = {
  ...heroSystemPalette,
  primary: palette.appleDark20,
  lightPrimary: palette.apple,
  hoverPrimary: palette.apple,
  activePrimary: palette.appleDark50,
  primaryBackground: palette.hitPink,
  primaryLightBackground: palette.hitPinkLight80,
  link: palette.appleDark20,
  hoverLink: palette.apple,
  activeLink: palette.appleDark50,
  infoDarkBackground: palette.maasstrichtBlueLight30,
};

export default jobsSystemPalette;
