import { eBensWebPalette as palette } from '@hero-design/colors';
import heroSystemPalette from './hero';
import type { SystemPalette } from './types';

const eBensSystemPalette: SystemPalette = {
  ...heroSystemPalette,
  activePrimary: palette.violetDark50,
  lightPrimary: palette.mauve,
  primaryBackground: palette.mauve,
  primaryLightBackground: palette.mauveLight80,
  activeLink: palette.violetDark50,
};

export default eBensSystemPalette;
