import theme from '..';
import omopTheme from '../omopTheme';
import { heroSystemPalette, omopSystemPalette } from '../global/colors';

describe('theme', () => {
  it('has correct hero theme config', () => {
    expect(theme).toMatchSnapshot();
  });

  it('has correct omop theme config', () => {
    expect(omopTheme).toMatchSnapshot();
  });
});

describe('theme', () => {
  it('has correct hero theme config', () => {
    expect(heroSystemPalette).toMatchSnapshot();
  });

  it('has correct omop theme config', () => {
    expect(omopSystemPalette).toMatchSnapshot();
  });
});
